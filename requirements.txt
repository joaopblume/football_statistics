# Runtime dependencies (top-level — pip resolves transitive deps).
# For an exact reproducible environment use:  pip install -r requirements.lock.txt
# Dev/test tools live in requirements-dev.txt.

# --- Orchestration (Apache Airflow 3.x + providers) ---
apache-airflow==3.1.7
apache-airflow-providers-postgres==6.6.0
apache-airflow-providers-standard==1.11.0
apache-airflow-providers-smtp==2.4.2          # failure notifications

# --- Extraction + object storage ---
soccerdata==1.8.8
boto3==1.42.61                                 # MinIO/S3 uploads (lib/minio_config, extraction_helpers)

# --- Data handling ---
pandas==2.3.3
pendulum==3.2.0

# --- PostgreSQL (control plane + serving) ---
psycopg2-binary==2.9.11

# --- Config + observability ---
python-dotenv==1.2.1
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
